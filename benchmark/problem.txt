# Benchmark 项目四轮代码审查（最新复核）

## Findings

- 本轮未发现新的功能性问题（no findings）。

---

## 回归验证结果

已复核关键路径并通过最小复现实验：

1. `parser_error` 口径一致性
   - `stats["models"]` 与 `stats["models_by_rounds"]` 的 adjusted completion 一致。
2. 失败分类正确性
   - `parse_ok=False` 且发生编译失败时，归类为 `compile_error`（不再误计为 infra）。
3. dry-run 语义正确
   - `skip_compile=True` 时仅执行 1 轮生成并停止。
4. 语法检查
   - `python3 -m py_compile harness/*.py scripts/*.py` 通过。

---

## Residual Risk（测试缺口）

当前仓库仍缺少自动化回归测试来锁定上述行为，后续修改存在回归风险。
建议补至少 3 个最小单测：

1. `metrics`：同一输入下 `models` 与 `models_by_rounds` 的 adjusted completion 必须一致。
2. `runner`：`skip_compile=True` 时只运行 1 轮。
3. `problem_extractor`：单行 `:= by trivial` 能正确提取 `ground_truth`。

